# gmenu-chat allowlisted tools (Phase B)

Default model: abliterated / offline (probed from Ollama tags).

```bash
gmenu-chat --list-tools
gmenu-chat --run status
gmenu-chat --run local_engine_status
gmenu-chat --run env_scan
gmenu-chat --run engine_repair --confirm
```

Power: `loc` multi-tool agent. Safe: `gmenu-chat` allowlist only.
